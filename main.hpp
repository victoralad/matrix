#ifndef MATRIX_H
#define MATRIX_H
#include <iostream>
#include <vector>
#include <string>

class Victor
{
    public:
    //void *multiply(void *m1, int row1, int col1, void *m2, int row2, int col2);
    int transpose(const vector<int> & a);
};

#endif